<sd-content>

    <sd-article if={ currentPageType === 'article' }></sd-article>
    <sd-namespace if={ currentPageType === 'namespace' }></sd-namespace>
    <sd-type if={ currentPageType === 'type' }></sd-type>
    
</sd-content>